info:
  name: List Accounts
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
