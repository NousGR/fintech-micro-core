info:
  name: Invalid Transfer Limit
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/transfers?limit=0"
  params:
    - name: limit
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
