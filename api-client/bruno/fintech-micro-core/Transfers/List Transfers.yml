info:
  name: List Transfers
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/transfers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
