info:
  name: List Transfers Limit
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/transfers?limit=3"
  params:
    - name: limit
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
