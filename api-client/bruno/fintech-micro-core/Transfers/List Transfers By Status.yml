info:
  name: List Transfers By Status
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/transfers?status=COMPLETED"
  params:
    - name: status
      value: COMPLETED
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
